package Strings;

public class Foundcharboolean {
    public static void main(String[] args) {
        String str = "programming";
        String result =" ";
        for(int i = 0;i<str.length();i++){
            char ch = str.charAt(i);
            boolean found = false;

            
            if(found == false) {
                result = result + ch;
            }
        }
        System.out.println(result);
        
        
    }
}
